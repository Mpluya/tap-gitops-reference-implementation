---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: amr-observer-editor-token-test
  namespace: amr-observer-system
spec:
  secretStoreRef:
    name: cluster-secret-store
    kind: ClusterSecretStore
  refreshInterval: "1m"

  target:
    template:
      data:
        ca.crt: "{{ .amr_observer_ca_crt }}"
        token: "{{ .amr_observer_auth_token }}"
  data:
  - secretKey: amr_observer_ca_crt
    remoteRef:
      key: dev/tap-build/tap/tenant-install-secrets
      property: amr_observer_ca_crt
  - secretKey: amr_observer_auth_token
    remoteRef:
      key: dev/tap-build/tap/tenant-install-secrets
      property: amr_observer_auth_token